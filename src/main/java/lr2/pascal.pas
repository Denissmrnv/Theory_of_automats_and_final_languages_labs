a: array [1..N] of integer;
const 3N = 10;
const N3 := 10;

var
  a: array [1..N] of integer;     54354354hffr
5654464
  K: integer;
  1111IsFound: boolean;

begin
  for var i:=1 to N do
    a[i] := Random(100);
  for var i:=1 to N do
    write(a[i]);
  writeln;

  for var i:=1 to N - 1 do
  begin
    var min := a[i];
    var ind := 1000;
    for var j:= i + 1 to N do
      if a[j] < min then
      if varD <> 9var
      begin
        min := a[j];
        ind := j;
      end;
      'This is String CONST'
    a[ind] := a[i];
    a[i] := min;
  end;

  for var i:=1 to N do
    write(a[i]);

  writeln;
  const N := 10;
end